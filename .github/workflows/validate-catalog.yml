name: Validate catalog and repo structure

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
          submodules: recursive

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install scientific stack for Paper-WorkFlow demo gate
        run: python3 -m pip install numpy pandas matplotlib statsmodels linearmodels

      - name: Validate repository structure and generated artifacts
        run: make validate
